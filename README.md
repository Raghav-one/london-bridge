# London Bridge

A single-file, interactive 15-day work-trip companion for London and Edinburgh.

Open `index.html` directly or serve the directory locally:

```sh
python3 -m http.server 4180 --directory london-bridge
```

The guide has no build step or external runtime dependencies. It stores checklist choices, itinerary settings, shortlists, budget inputs and notes in the current browser only.
